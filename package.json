{
  "name": "agente-empleo",
  "version": "0.1.0",
  "private": true,
  "description": "Agente local de búsqueda de empleo (Next.js + Playwright + Anthropic)",
  "scripts": {
    "dev": "npm run dev --prefix ui",
    "build": "npm run build --prefix ui",
    "lint": "npm run lint --prefix ui",
    "generate-cv": "npm run generate-cv --prefix ui",
    "apply": "npm run apply --prefix ui",
    "test": "npm run test --prefix ui",
    "postinstall": "npm install --prefix ui"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.62.0",
    "dotenv": "^17.2.1",
    "googleapis": "^156.0.0",
    "mammoth": "^1.10.0",
    "playwright": "^1.55.0",
    "unpdf": "^1.1.0",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.5",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
